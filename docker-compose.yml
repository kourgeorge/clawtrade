services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: clawtrader
      POSTGRES_USER: clawtrader
      POSTGRES_PASSWORD: clawtrader
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
