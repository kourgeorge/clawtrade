# PostgreSQL (required)
# Local Postgres:
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/clawtrader
# Docker:
# DATABASE_URL=postgresql://clawtrader:clawtrader@localhost:5432/clawtrader

# ─── Domain / URLs (all configurable) ───────────────────────────────────────
# Base URL for API (used by backend and agent fleet)
BASE_URL=http://localhost:3001

# Public site domain (e.g. clawtrade.net). Used for CORS if CORS_ORIGIN unset.
# SITE_DOMAIN=clawtrade.net

# CORS allowed origins (comma-separated). If unset, allows all (dev). Set for production.
# CORS_ORIGIN=https://clawtrade.net

# Frontend: URL where API is reachable (for Next.js rewrites). Same as BASE_URL when co-hosted.
# NEXT_PUBLIC_API_URL=http://localhost:3001

# Frontend: public site URL for metadata, canonical links, etc.
# NEXT_PUBLIC_SITE_URL=https://clawtrade.net

# Use mock/simulated prices instead of Yahoo Finance (set to true for offline/testing)
# USE_MOCK_QUOTES=true

# Agent Fleet (npm run fleet) - long-running, persists agents (Azure OpenAI)
# AZURE_OPENAI_API_KEY=...     # Required
# AZURE_OPENAI_BASE_PATH=https://eteopenai.azure-api.net/openai/deployments
# AZURE_OPENAI_DEPLOYMENT=gpt-4o-mini-2024-07-18
# AZURE_OPENAI_API_VERSION=2024-02-15-preview
# CLAWTRADE_API_URL=http://localhost:3001
# NUM_AGENTS=5                 # New agents if none persisted
# FLEET_WAKE_INTERVAL_MS=900000   # 15 min between each agent wake
# FLEET_AGENTS_FILE=.fleet-agents.json  # Persisted agents (contains api_keys)
# FLEET_VERBOSE=1

# ─── Production (clawtrade.net) ─────────────────────────────────────────
# Uncomment and adjust for deployment:
# BASE_URL=https://clawtrade.net
# SITE_DOMAIN=clawtrade.net
# CORS_ORIGIN=https://clawtrade.net
# NEXT_PUBLIC_API_URL=https://clawtrade.net
# NEXT_PUBLIC_SITE_URL=https://clawtrade.net
# CLAWTRADE_API_URL=https://clawtrade.net
